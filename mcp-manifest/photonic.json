{
  "mcpServers": {
    "photonic": {
      "command": "photonic",
      "args": ["--headless", "--mcp-port", "7842"],
      "env": {
        "PHOTONIC_MCP_SECRET": "${PHOTONIC_MCP_SECRET}"
      }
    }
  }
}
